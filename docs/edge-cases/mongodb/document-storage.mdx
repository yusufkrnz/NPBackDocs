---
title: Doküman Boyutu ve TTL
sidebar_position: 1
description: real_rows'un conversation dokümanına gömülmesi ve TTL sonrası referans erişimiyle ilgili 2 uç durum — henüz hiçbiri çözülmedi.
---

import CodeEditor from '@site/src/components/CodeEditor';

# Doküman Boyutu ve TTL

Doğru pattern: `conversations` koleksiyonu küçük ve kalıcı kalmalı, büyük/geçici veri (`real_rows`) ayrı bir `query_results` koleksiyonunda, TTL index ile otomatik silinerek durmalı. Aşağıdaki 2 case, bu ayrım yapılmazsa/eksik yapılırsa ortaya çıkar.

## EDGE-CASE-012: `real_rows` Conversation Dokümanına Gömülürse 16MB Limiti Riski

**Risk: Yüksek.** Query sonuçları doğrudan conversation dokümanına gömülürse (embed), MongoDB'nin 16MB doküman limitine yaklaşılır.

<CodeEditor
  filename="senaryo"
  highlightLines={[6, 9]}
  code={`# conversation dokümanına DOĞRUDAN gömülüyor
await db.conversations.update_one(
    {"_id": conv_id},
    {"$push": {"turns": {
        "user_message": message,
        "result": real_rows,          # binlerce satır, doğrudan embed
    }}},
)
# N. turn'den sonra: pymongo.errors.DocumentTooLarge`}
/>

## EDGE-CASE-013: Süresi Dolmuş Sonuç Referansına Erişim

**Risk: Orta.** `query_results` koleksiyonu TTL ile (örn. 24 saat sonra) otomatik siliniyorsa, kullanıcı "az önceki tabloyu tekrar göster" dediğinde `result_ref` artık DB'de yoktur — bu durum ele alınmazsa ham bir `TypeError` kullanıcıya kadar sızar.

<CodeEditor
  filename="senaryo"
  highlightLines={[4, 5]}
  code={`result_ref = turn["result_ref"]
result = await db.query_results.find_one({"_id": result_ref})
# TTL süresi dolmuşsa result artık None
rows = result["rows"]
# -> TypeError: 'NoneType' object is not subscriptable
# Kullanıcıya "sonuç süresi doldu" yerine ham hata dönüyor`}
/>
