---
title: Şifre Sıfırlama
sidebar_position: 3
description: Şifremi unuttum akışı — reset token üretimi, e-posta, yeni şifre belirleme.
---

# Şifre Sıfırlama

## Akış

```mermaid
sequenceDiagram
    participant Client
    participant Auth as Auth Service
    participant DB
    participant Mail as E-posta servisi

    Client->>Auth: POST /auth/forgot-password {email}
    Auth->>DB: kullanıcı var mı kontrol et
    Auth->>DB: reset token üret (hash'lenmiş, TTL 15 dk)
    Auth->>Mail: sıfırlama linki gönder
    Auth-->>Client: 200 (kullanıcı var/yok fark etmeksizin aynı yanıt)

    Client->>Auth: POST /auth/reset-password {token, new_password}
    Auth->>DB: token hash'ini doğrula (süre + is_used)
    Auth->>DB: password_hash güncelle, token'ı is_used=true yap
    Auth->>DB: kullanıcının TÜM refresh token'larını is_revoked=true yap
    Auth-->>Client: 200
```

## Üretim

```python
# services/token_service.py
PASSWORD_RESET_TTL_MINUTES = 15


async def issue_password_reset_token(db, user_id: str) -> str:
    raw_token, token_hash = create_refresh_token()  # aynı üretim şekli: random + hash
    await db.execute(
        insert(PasswordResetTokens).values(
            user_id=user_id,
            token_hash=token_hash,
            expires_at=datetime.now(timezone.utc) + timedelta(minutes=PASSWORD_RESET_TTL_MINUTES),
            is_used=False,
        )
    )
    return raw_token
```

## Notlar

- `/auth/forgot-password` her zaman `200` döner — kullanıcı DB'de olsa da olmasa da. Aksi hâlde yanıt farkından hangi e-postaların kayıtlı olduğu (user enumeration) anlaşılabilir.
- Reset token tek kullanımlıktır (`is_used`), refresh token gibi rotasyona değil **tek seferlik tüketime** göre tasarlanır — TTL de kısa (15 dk), refresh token'ın 30 gününden farklı.
- Şifre değiştiğinde kullanıcının tüm refresh token'ları iptal edilir — hesap ele geçirilmişse, şifre sıfırlanınca saldırganın elindeki refresh token de geçersiz kalır.
